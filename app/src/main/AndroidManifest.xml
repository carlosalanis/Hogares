<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lavid.hogares">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".planActivity"
            android:label="@string/title_activity_plan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".readerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:label="@string/title_activity_main"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".sermonMenu"
            android:label="@string/title_activity_sermon_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".oracionesMenu"
            android:label="@string/title_activity_oraciones_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".chapters"
            android:label="@string/title_activity_chapters"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>